import math

r = float(input("請輸入圓半徑："))

if r >= 0:
    area = math.pi * r * r
    perimeter = 2 * math.pi * r
    print(f"面積 = {area:.2f}")
    print(f"周長 = {perimeter:.2f}")
else:
    print("半徑不能為負數")
