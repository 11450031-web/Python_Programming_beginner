#include <stdio.h>
#define PI 3.14159

int main() {
    float r;
    scanf("%f", &r);

    if (r >= 0) {
        printf("面積 = %.2f\n", PI * r * r);
        printf("周長 = %.2f\n", 2 * PI * r);
    } else {
        printf("半徑不能為負數\n");
    }
    return 0;
}
